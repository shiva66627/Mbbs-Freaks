name: demo2
description: "A new Flutter project."
publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Firebase & Google Sign-In
  firebase_core: ^3.4.1
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.3.4
  google_sign_in: ^6.2.1
  flutter_signin_button: ^2.0.0
  url_launcher: ^6.3.1
  webview_flutter: ^4.4.2

  # HTTP for API calls
  http: ^1.1.0
  dio: ^5.4.0
  pdfx: ^2.6.0 
  razorpay_flutter: ^1.3.5

  
  # 👈 add this


  # Google APIs
  googleapis: ^13.2.0
  googleapis_auth: ^1.6.0

  # PDF viewing and downloading
  flutter_pdfview: ^1.3.2
  path_provider: ^2.1.2
  permission_handler: ^11.3.1  # Added for PDF file selection

  # Loading and caching
  cached_network_image: ^3.3.1

  # Local storage
  shared_preferences: ^2.2.2

  # Cupertino icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/LOGGOS.jpeg
    - assets/logo.png
